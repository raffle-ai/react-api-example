const baseURL = "https://api.raffle.ai/v2";

export const routes = {
  topQuestionsURL: `${baseURL}/top_questions`,
  suggestionsURL: `${baseURL}/autocomplete`,
  searchURL: `${baseURL}/search`,
  summaryURL: `${baseURL}/summary`,
};
